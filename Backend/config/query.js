// Tu run queries on MYSQL database 
import {conn} from './db.js'

// conn.query("SELECT * FROM PROFILE;")

// export const queryDatabase = (query , params = [] )=>{
//     return new Promise((resolve , reject)=>{
//          conn.execute(query , params , (err , result)=>{
//             if(err){
//                 reject(err);
//                 return;
//             }
//             return resolve(result);
//          })
//     })
// }

